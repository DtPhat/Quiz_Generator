import React from "react";
export default function(){
    <button key={answer} className="answer">
                {he.decode(answer)}
            </button>
}

const answerElements = arrayAnswers.map((answer) => (
    <Answer
        key= {answer}
        ans={answer}
    />
))